import App from "./pages/App.svelte";

const app = new App({
  target: document.body,
});

export default app;

// import SigninPage from './pages/Signup.svelte';

// const app = new SigninPage({
// 	target: document.body
// });

// export default app;
